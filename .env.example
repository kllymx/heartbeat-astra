# Server-only credentials. Never use a VITE_ prefix for these values.
OPENAI_API_KEY=
# Set the exact Responses API model ID enabled for your API project.
# No model is selected implicitly, including for the Astra hackathon.
OPENAI_MODEL=
